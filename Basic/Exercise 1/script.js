function crytalGazer(children, partner, geo, job){
    let val = `You will be a ${job} in ${geo} and married to ${partner} with ${children} childrens.`;
    return val;
}

let result = crytalGazer(2, "Marie", "Vienna", "Programmer");

console.log(result);

